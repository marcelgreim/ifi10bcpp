#include <iostream>
#include <iomanip>

using namespace std;

int pi (int); // Compiler über unsere Funktion gleich am Anfang Bescheid geben

int main () {
	int eingabe; // Initialisieren der Variable eingabe
	
	cout << "Wie weit soll die Reihe für PI gebildet werden?" << endl; // Abfrage an den Nutzer, wie weit die Reihe gebildet werden soll
	cin >> eingabe; // Einlesen der Nutzerabfrage in die Variable eingabe
	
	cout << "Die PI Reihe ist: " << pi(eingabe) << endl;
	}
	
int pi (int eing) {
	int counter;
	int rechnung;
	int i = 3;
	counter = eing;
	
	for(eing; eing >= 1; eing--) {
		
	
	
	rechnung=1-1/3+1/3+2*4;
	
	rechnung2=rechnung2+
	
}
	return rechnung;
	
}
